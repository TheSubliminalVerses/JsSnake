declare module 'index' {
    global {
        const Scl: number
        const gameOverWindow: HTMLElement
        const scoreField: HTMLElement

        let GameOver: boolean
        let Score: number
    }
}